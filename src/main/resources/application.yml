server:
  port: '9090'
  servlet:
    context-path: '/personigramaunibagueBackend'
spring:
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:5432/postgres'
    username: uiperson
    password: 'P88K5pKrDFP5%ES#/d'
    hikari:
      connectionTimeout: '20000'
      maximumPoolSize: '5'